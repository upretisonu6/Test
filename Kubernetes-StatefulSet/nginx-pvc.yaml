apiVersion: v1  
kind: PersistentVolumeClaim  
metadata:
  name: nginx-pvc  
spec:
  accessModes:
    - ReadWriteOnce  # used by node at a time                  
  resources:
    requests:
      storage: 1Gi  
  storageClassName: manual  # ref by pv bound
                            
